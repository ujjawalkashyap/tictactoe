package src;

class GameResult {
    boolean isComplete;
    String winner;

    GameResult(boolean isComplete, String winner) {
        this.isComplete = isComplete;
        this.winner = winner;
    }
}
